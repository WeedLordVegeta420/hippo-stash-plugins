{
  "name": "sprite-tab",
  "version": "1.0.1",
  "description": "Stash plugin that adds a tab displaying sprite sheets",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "sync": "test -n \"$STASH_PLUGIN_DIR\" || (echo 'Error: STASH_PLUGIN_DIR not set' && exit 1) && mkdir -p \"$STASH_PLUGIN_DIR\" && cp sprites.js SpriteTab.yml \"$STASH_PLUGIN_DIR/\""
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
